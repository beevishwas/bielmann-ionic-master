//
// ===== File globals.ts    
//
export const GlobalVariable = Object.freeze({
    CookieName: {
        baseUrl: "BM_SSL_URL",
        xToken: "xToken",
        userId: "userId",
        userDetail:"userDetail"
    }
});